on:
  push:
    branches:
      - master




jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3 

      - name: "Login to Docker hub" 
        run: pwd


      - name: "print" 
        run: echo Reham
        


     
